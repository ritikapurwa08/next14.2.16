import SetupShowCasePage from "@/components/setups/setup-page";
import React from "react";

const page = () => {
  return (
    <div>
      <SetupShowCasePage />
    </div>
  );
};

export default page;
